import "../styles/nav.css"

import React from "react";

const Nav = () => {
    return (
        <header>
            <nav className="Nav">
                <h2>library-db</h2>
            </nav>
        </header>
    );
}

export default Nav;